specVersion: "v1beta"
serviceId: "billing-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "billing-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
